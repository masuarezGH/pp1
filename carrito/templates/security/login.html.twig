{% extends 'base.html.twig' %}

{% block title %}Login{% endblock %}

{% block body %}
    <div style="background-color: black; color: white; padding: 15px; font-weight: bold;">
        Carrito de Compras
    </div>

    <div style="display: flex; justify-content: center; align-items: center; height: 80vh;">
        <div style="border: 1px solid #ccc; padding: 30px; border-radius: 8px; width: 300px; background: #fff; box-shadow: 0px 2px 6px rgba(0,0,0,0.2);">
            <h2 style="text-align: center; margin-bottom: 20px;">Autenticación</h2>

            {% if error %}
                <div style="color: red; margin-bottom: 10px;">
                    {{ error.messageKey|trans(error.messageData, 'security') }}
                </div>
            {% endif %}

            <form action="{{ path('app_login') }}" method="post" style="display: flex; flex-direction: column; gap: 10px;">
                <label for="username">Email:</label>
                <input type="text" id="username" name="_username" value="{{ last_username }}" 
                       style="padding: 5px; border: 1px solid #ccc; border-radius: 4px;">

                <label for="password">Password:</label>
                <input type="password" id="password" name="_password" 
                       style="padding: 5px; border: 1px solid #ccc; border-radius: 4px;">

                <button type="submit" 
                        style="margin-top: 15px; padding: 8px; border: none; background-color: black; 
                               color: white; border-radius: 4px; cursor: pointer; font-weight: bold;">
                    login
                </button>
            </form>
        </div>
    </div>
{% endblock %}